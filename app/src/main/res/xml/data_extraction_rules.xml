<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules (Android 12+). Controls what can be:
      - cloud-backup: backed up to Google Drive (Auto Backup)
      - device-transfer: copied to a new device during Android setup

    The manifest sets android:allowBackup="false", but the device-transfer path is
    a separate channel that still works without that flag — so we explicitly exclude
    sensitive paths here too. Defense in depth.

    See: https://developer.android.com/about/versions/12/backup-restore#xml-changes
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </device-transfer>
</data-extraction-rules>

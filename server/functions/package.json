{
  "name": "mealtime-api-proxy",
  "description": "Server-side proxy for MealTime — keeps Spoonacular / Gemini / Groq API keys off the client (Firebase Cloud Functions, 2nd gen).",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "type": "module",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
